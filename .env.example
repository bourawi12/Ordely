# Copy to .env to override docker compose defaults.
POSTGRES_USER=ordely
POSTGRES_PASSWORD=ordely
POSTGRES_DB=ordely

# Required: secret used to sign login tokens (openssl rand -hex 32)
JWT_SECRET=
# Token lifetime in seconds
JWT_EXPIRES_IN=86400

# Host ports
DB_PORT=5433
BACKEND_PORT=3001
FRONTEND_PORT=3200
